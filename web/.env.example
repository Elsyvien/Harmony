VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000/ws

# Optional: Dedicated TURN server for WebRTC behind restrictive NATs.
# If omitted, the app uses a minimal public fallback TURN for development only.
# For production, set all three values below.
# VITE_TURN_URLS=turn:your-turn-server.com:3478,turns:your-turn-server.com:5349
# VITE_TURN_USERNAME=username
# VITE_TURN_CREDENTIAL=credential
